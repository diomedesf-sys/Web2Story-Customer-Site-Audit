services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: w2s-eval
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
